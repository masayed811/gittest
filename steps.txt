1. git clone url ( this will clone project from cloud to your local machine )
2. git status (should show changes on your local repository )
3. git add ( to make the files to make its unstage to stage )


 3) git config --local user.email "sayed811@gmail.com"


 4) git config --local user.name "masayed811"


5) git add .




 7) git status

 8) git commit -m 'mess' ( messege could be any like:  added steps)


 9) git push origin master

 8)  git remort add origin url